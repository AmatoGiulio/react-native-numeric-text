export { NumericTextView } from './NumericTextView';
/** Preferred name. `NumericTextView` is the same component, kept for the original import path. */
export { NumericTextView as NumericText } from './NumericTextView';
export type { NumericTextProps } from './types';
